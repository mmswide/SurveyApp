<% provide(:title, "Delete Ticket") %>

<%= link_to('<< back to ticket list',  {:action => 'index', :event_id => @event.id} , :class => '')%>

<h1>Delete Ticket</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(:event, :url => {:action => 'destroy', :id => @ticket.id, :event_id => @event.id})do |f| %>
  		<center> 
     		<p>Are you use you want to delete this ticket?</p>
    		 <h3> <%= @ticket.ticket_name %> <h3>
 		</center>
      <%= submit_tag("Delete Ticket", class: "btn btn-danger") %>
    <% end %>
  </div>
</div>

